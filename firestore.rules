rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Só usuários autenticados (login feito na tela do CRM) podem ler/escrever.
    match /contacts/{contactId} {
      allow read, write: if request.auth != null;
    }
  }
}
